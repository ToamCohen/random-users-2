class LocalStorage {
	constructor(id, mainUser, qoutes, pokemon, text, friends) {
		(this.id = id), (this.mainUser = mainUser);
		(this.qoutes = qoutes), (this.pokemon = pokemon);
		(this.text = text), (this.friends = friends);
	}
}
